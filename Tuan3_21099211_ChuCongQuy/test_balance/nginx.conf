events {}

http {
    upstream backend {
        server web_server_1:3000;
        server web_server_2:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}
